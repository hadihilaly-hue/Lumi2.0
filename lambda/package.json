{
  "name": "lumi-claude-proxy",
  "version": "1.0.0",
  "description": "Lumi backend Lambda — Bedrock proxy, S3 signed URLs, RDS data routes.",
  "type": "module",
  "main": "index.mjs",
  "private": true,
  "scripts": {
    "test": "node --import ./test/register.mjs --test \"test/*.test.mjs\""
  },
  "dependencies": {
    "@aws-sdk/rds-signer": "^3.726.0",
    "aws-jwt-verify": "^5.2.1",
    "pg": "^8.13.1"
  }
}
